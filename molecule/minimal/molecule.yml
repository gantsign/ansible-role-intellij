---
dependency:
  name: galaxy

driver:
  name: docker

role_name_check: 2

platforms:
  - name: ansible-role-intellij-minimal
    image: ubuntu:22.04

provisioner:
  name: ansible

verifier:
  name: testinfra
